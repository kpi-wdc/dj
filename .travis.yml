language: java
env:
  - MINIFY_CODE=true MERGE_JS=true
  - MERGE_JS=true
  - MINIFY_CODE=true
  -

before_script:
  - psql -c 'create database wdc2;' -U postgres
  - psql -f wdc2.sql -U postgres -d wdc2

